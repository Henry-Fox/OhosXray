# Place prebuilt `libxraycore.so` here (and under `entry/src/main/cpp/libs/arm64-v8a/`).
#
# Build:
#   cd native-core/go-shim
#   powershell -ExecutionPolicy Bypass -File .\build_ohos_arm64.ps1
#
# Or download the `.so` from GitHub Releases.
